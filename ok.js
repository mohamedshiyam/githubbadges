class Generator {

    constructor(inputFile, outputDir) {
        this.inputFile = inputFile;
    
    }
    
    class Generator {

    constructor(inputFile, outputDir) {
        this.inputFile = inputFile;
    
    }

class Generator {

    constructor(inputFile, outputDir) {
        this.inputFile = inputFile;
    
    }
    
    class Generator {

    constructor(inputFile, outputDir) {
        this.inputFile = inputFile;
    
    }

